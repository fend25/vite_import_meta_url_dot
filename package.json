{
  "name": "vite_importmetaurl_test",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "vite --force",
    "build": "rimraf dist && vite build",
    "webpack": "rimraf dist && webpack build",
    "serve": "serve dist",
    "preview": "vite preview"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "serve": "^13.0.2",
    "vite": "^2.8.0"
  },
  "dependencies": {
    "lib_importmetaurl_dot": "^0.0.1"
  }
}
